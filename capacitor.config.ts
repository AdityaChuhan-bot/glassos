import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.glassyos.launcher',
  appName: 'GlassyOS Launcher',
  webDir: 'dist'
};

export default config;
